name: Build Godot Android APK
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
env:
  GODOT_VERSION: '4.6'
  GODOT_PLATFORM: 'linux.x86_64'
  ANDROID_HOME: '/opt/android-sdk'
jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      # 1. 拉取代码
      - name: Checkout
        run: |
          git clone --depth=1 https://github.com/pipos52/kashi-simulator.git repo
          cd repo
          echo "Checked out: $(git rev-parse --short HEAD)"
          echo "Files: $(ls -la)"
        
      # 2. 下载 Godot 引擎
      - name: Download Godot
        run: |
          cd ${{ github.workspace }}/repo
          wget -q https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_${GODOT_PLATFORM}.zip -O godot.zip
          unzip -q godot.zip
          chmod +x Godot_v${GODOT_VERSION}-stable_${GODOT_PLATFORM}
          
      # 3. 下载导出模板
      - name: Download Export Templates
        run: |
          cd ${{ github.workspace }}/repo
          wget -q https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_export_templates.tpz -O templates.tpz
          mkdir -p ~/.local/share/godot/export_templates/${GODOT_VERSION}.stable
          unzip -q templates.tpz -d /tmp/templates
          mv /tmp/templates/templates/* ~/.local/share/godot/export_templates/${GODOT_VERSION}.stable/
          
      # 4. 安装 Java 17（强制安装，替换默认Java11）
      - name: Setup Java
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y openjdk-17-jdk > /dev/null 2>&1 || sudo apt-get install -y openjdk-17-jdk-headless > /dev/null 2>&1 || true
          which java && java -version 2>&1 | head -3
          /usr/lib/jvm/java-17-openjdk-amd64/bin/java -version 2>&1 | head -3 || true
          echo "JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" >> $GITHUB_ENV
          echo "PATH=/usr/lib/jvm/java-17-openjdk-amd64/bin:\$PATH" >> $GITHUB_ENV
          
      # 5. 安装 Android SDK（使用 shell 内建命令）
      - name: Setup Android SDK
        run: |
          export ANDROID_HOME=/opt/android-sdk
          command mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip
          command mv cmdline-tools latest
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager --install "platforms;android-34" "build-tools;34.0.0" "platform-tools"
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools" >> $GITHUB_ENV
          sdkmanager --list_installed 2>&1 | head -10
        
      # 6. 生成调试密钥库
      - name: Create Debug Keystore
        run: |
          cd ${{ github.workspace }}/repo
          keytool -genkey -v -keystore debug.keystore -alias debugkey -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=Debug, O=Debug, C=US"
          ls -la debug.keystore
            
      # 7. 检查项目结构
      - name: Check Project
        run: |
          cd ${{ github.workspace }}/repo
          echo "=== Project root ==="
          ls -la
          echo ""
          echo "=== export_presets.cfg ==="
          cat export_presets.cfg | head -15
          echo ""
          echo "=== android folder ==="
          ls -la android/ 2>/dev/null || echo "No android folder"
          echo ""
          echo "=== project.godot ==="
          head -5 project.godot
          
      # 8. 构建 APK
      - name: Build APK
        run: |
          cd ${{ github.workspace }}/repo
          export ANDROID_HOME=${{ env.ANDROID_HOME }}
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$JAVA_HOME/bin:$PATH
          echo "JAVA_HOME=$JAVA_HOME"
          java -version 2>&1 | head -3
          echo "ANDROID_HOME=$ANDROID_HOME"
          mkdir -p android/build
          ./Godot_v${{ env.GODOT_VERSION }}-stable_${{ env.GODOT_PLATFORM }} --headless --export-release "KashiAndroid" android/build/KashiAndroid.apk 2>&1
          echo ""
          echo "=== Build artifacts ==="
          find . -name "*.apk" -type f 2>/dev/null
          ls -la android/build/ 2>/dev/null || true
          
      # 9. 上传 APK
      - name: Upload APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: godot-android-apk
          path: |
            repo/**/*.apk
            repo/android/build/*.apk
